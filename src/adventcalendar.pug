include includes/mixins

block vars
  - const pageTitle = "Calendario dell'Avvento"
  - const pageDescription = "🎄 Vi auguriamo un felice Natale con il nostro albero! 🎅"
  - const pageCard = "/img/card/adventskalender.png"

<!DOCTYPE html>
html(lang="it")
  head
    include includes/_head
    include includes/_adventcalendar
    link(href=assetPath("/css/adventcalendar.css") rel="stylesheet")
  body
    main.main
      //-p
        | Vi auguriamo un felice Natale
        br
        | con il nostro albero! 🎅-//
      #tree.tree
        each i in Array.from({ length: 21 }, (_, i) => i)
          - const [block, url] = Object.entries(adventcalendar[i])[0] || []
          ul.cube
            li
            li
            li
              if block && url
                a(href=url rel="nofollow noopener" target="_blank")!= block.slice(0, 3) + "<br>" + block.slice(3)
            li
        ul.cube.logo
          li
          li
          li: a(href="/"): +sprite("logo-square").logo
          li
      //-p
        = "Danke validblock"
        br
        = " für den schönen Baum! 🎄"
      //-a(href="/") Zurück zur Homepage

    include includes/_footer
    